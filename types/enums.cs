namespace export { 
	public enum mergeType {Across, Down} 
	public enum excelStyle {Default, Header, Bold, Good, Neutral, Bad, Wrap, Gray, Orange, RedText } 
}
namespace secureWipe { 
	public enum wipeType {Random, Zero, One} 
}
namespace imagePatcher {
	public enum registryTypes {String, Binary, DWord, QWord}
}